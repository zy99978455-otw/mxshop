services:
  # ----------------------------------------------------
  # 1. Go 服务层 (用户服务)
  # ----------------------------------------------------
  user_srv:
    image: golang:latest
    container_name: mxshop_user_srv
    volumes:
      - ./mxshop_srv:/mxshop_srv
    working_dir: /mxshop_srv/user_srv/
    environment:
      MYSQL_HOST: mysql_db
      MYSQL_PORT: 3306
      MYSQL_USER: root
      MYSQL_PASSWORD: 123456
      MYSQL_DATABASE: mxshop_user_srv
    # ports:
    #   - "8081:8081"
    # command: go run main.go # 容器启动时执行，路径在working_dir中
    command: tail -f /dev/null #保持容器不關閉
    networks:
      - mxshop_network
    # depends_on:
    #   - mysql_db
      
    tty: true

  # ----------------------------------------------------
  # 2. 用户 Web/API 层
  # ----------------------------------------------------
  user_web:
    image: golang:latest
    container_name: mxshop_user_api
    volumes:
      - ./mxshop_api:/mxshop_api
    working_dir: /mxshop_api/user_web/
    ports:
      - "8080:8080"
    environment:
      MXSHOP_SRV_HOST: user_srv
      MXSHOP_SRV_PORT: 50051
    command: tail -f /dev/null  # 建议加上这行，和 user_srv 保持一致
    networks:
      - mxshop_network
    depends_on:
      - user_srv
    tty: true

# ----------------------------------------------------
# 3. 网络定义
# ----------------------------------------------------
networks:
  mxshop_network:
    external: true
    name: mxshop_dev_network
